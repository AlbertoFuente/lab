define(function() {
    'use strict';

    return {
        init: function(components, api) {
            var particle = components.elementalParticle(api);
            console.log(particle);
        }
    };
}());
